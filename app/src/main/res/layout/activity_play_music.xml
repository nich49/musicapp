<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  android:id="@+id/activity_main"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:paddingBottom="@dimen/activity_vertical_margin"
  android:paddingLeft="@dimen/activity_horizontal_margin"
  android:paddingRight="@dimen/activity_horizontal_margin"
  android:paddingTop="@dimen/activity_vertical_margin"
  tools:context="com.example.urlconnection.showmusicfile.PlayMusicActivity">

  <Button
    android:id="@+id/button_audio_start"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="再生" />

  <Button
    android:id="@+id/button_audio_stop"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@+id/button_audio_start"
    android:text="終了"/>


  <Button
    android:id="@+id/button_audio_back"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@+id/button_audio_stop"
    android:text="５秒戻る"/>

  <Button
    android:id="@+id/button_double_speed"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@+id/button_audio_back"
    android:text="倍速"/>

  <SeekBar
    android:id="@+id/seekBar"
    android:layout_width="match_parent"
    android:layout_height="38dp"
    android:layout_below="@+id/button_double_speed"/>

  <TextView
    android:id="@+id/textView"
    android:layout_width="139dp"
    android:layout_height="49dp"
    android:layout_below="@+id/seekBar"
    android:text="再生時間"/>


  <Switch
    android:id="@+id/switchLoop"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@id/textView"
    android:text="音楽のループ"
    android:textOff="いいえ"
    android:textOn="はい"/>

</RelativeLayout>
